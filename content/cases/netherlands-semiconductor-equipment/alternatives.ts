/**
 * Netherlands × Semiconductor Equipment — alternative explanations.
 *
 * Deliberately EMPTY until Increment 4: an alternative explanation
 * requires sourced supporting AND opposing claims, and no claims exist
 * yet.
 */

import type { AlternativeExplanation } from '../../../lib/schemas.ts';

export const alternativeExplanations: AlternativeExplanation[] = [];
