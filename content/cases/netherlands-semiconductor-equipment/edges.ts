/**
 * Netherlands × Semiconductor Equipment — mechanism edges.
 *
 * Deliberately EMPTY until Increment 4: no edge may exist without a
 * sourced relational claim, and no claims exist yet.
 */

import type { MechanismEdge } from '../../../lib/schemas.ts';

export const edges: MechanismEdge[] = [];
