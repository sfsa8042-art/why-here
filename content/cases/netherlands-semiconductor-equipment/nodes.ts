/**
 * Netherlands × Semiconductor Equipment — mechanism nodes.
 *
 * Deliberately EMPTY until Increment 4: the Mechanism Map is generated
 * only from already-researched, classified claims, and no claims exist
 * yet.
 */

import type { MechanismNode } from '../../../lib/schemas.ts';

export const nodes: MechanismNode[] = [];
