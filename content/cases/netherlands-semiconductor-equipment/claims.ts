/**
 * Netherlands × Semiconductor Equipment — claims.
 *
 * Deliberately EMPTY until Increment 4. Claims are authored only
 * against real located sources supplied or verified by the maintainer;
 * placeholder claims to make the corpus look populated are prohibited
 * (a fabricated citation is the worst possible failure in this
 * project).
 */

import type { Claim } from '../../../lib/schemas.ts';

export const claims: Claim[] = [];
