/**
 * Netherlands × Semiconductor Equipment — sources.
 *
 * Deliberately EMPTY until Increment 4. Sources enter this file only
 * when real, retrievable documents have been located and verified by
 * the maintainer. Inventing sources — or copying plausible-looking
 * references without verification — is prohibited.
 */

import type { Source } from '../../lib/schemas.ts';

export const sources: Source[] = [];
